<!DOCTYPE html>
<html>
<head>
    <style>
        .header { color: #333; padding: 20px; }
        .content { padding: 20px; }
        .item-table { width: 100%; border-collapse: collapse; }
        .item-table th, .item-table td { padding: 10px; border: 1px solid #ddd; }
        .item-table th { background-color: #f5f5f5; }
        .summary { background-color: #f9f9f9; padding: 15px; margin: 20px 0; }
        .footer { padding: 20px; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="header">
        <h2>Purchase Order Confirmed</h2>
    </div>
    <div class="content">
        <p>Dear {{userName}},</p>
        
        <p>The purchase order has been confirmed by the dealer with pricing details:</p>

        <p><strong>Purchase ID:</strong> {{purchaseId}}</p>
        <p><strong>Order Date:</strong> {{orderDate}}</p>
        <p><strong>Confirmation Date:</strong> {{confirmationDate}}</p>
        <p><strong>Store:</strong> {{storeName}}</p>
        <p><strong>Dealer:</strong> {{dealerName}}</p>

        <h3>Confirmed Items:</h3>
        <table class="item-table">
            <thead>
                <tr>
                    <th>Explosive Type</th>
                    <th>Quantity</th>
                    <th>Unit Price</th>
                    <th>Total Price</th>
                    <th>Batch Number</th>
                </tr>
            </thead>
            <tbody>
                {{#each items}}
                <tr>
                    <td>{{explosiveType}}</td>
                    <td>{{quantity}}</td>
                    <td>{{unitPrice}}</td>
                    <td>{{totalPrice}}</td>
                    <td>{{batchNumber}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <div class="summary">
            <h3>Order Summary</h3>
            <p><strong>Total Amount:</strong> {{totalAmount}}</p>
            <p><strong>Payment Method:</strong> {{paymentMethod}}</p>
        </div>
    </div>
    <div class="footer">
        <p>This is an automated message. Please do not reply to this email.</p>
    </div>
</body>
</html>